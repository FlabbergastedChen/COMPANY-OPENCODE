---
description: "根据 tasks 执行实现并持续更新任务进度。"
---

你正在执行命令 `/openspec-apply`。

用户输入参数：`$ARGUMENTS`

执行要求：
1. 先对齐本命令目标：根据 tasks 执行实现并持续更新任务进度。
2. 优先按技能 `openspec-apply-change` 的工作方式执行。
3. 若参数缺失，基于上下文做最小假设并明确写出假设。
4. 若出现阻塞，输出阻塞原因、影响范围与建议解法，不做无依据猜测。

输出要求：
- 代码改动
- 任务完成进度
- 阻塞说明（如有）
- 输出简短执行摘要（已完成 / 未完成 / 风险）。

建议下一步：
- `/openspec-verify`
