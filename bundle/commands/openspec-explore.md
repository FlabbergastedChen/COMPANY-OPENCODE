---
description: "在创建变更前先探索需求、风险和备选方案。"
---

你正在执行命令 `/openspec-explore`。

用户输入参数：`$ARGUMENTS`

执行要求：
1. 先对齐本命令目标：在创建变更前先探索需求、风险和备选方案。
2. 优先按技能 `openspec-explore` 的工作方式执行。
3. 若参数缺失，基于上下文做最小假设并明确写出假设。
4. 若出现阻塞，输出阻塞原因、影响范围与建议解法，不做无依据猜测。

输出要求：
- 探索结论
- 推荐方案与放弃方案
- 建议下一步：`/openspec-propose` 或 `/openspec-new`
- 输出简短执行摘要（已完成 / 未完成 / 风险）。
