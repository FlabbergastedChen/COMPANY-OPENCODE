---
description: "开发完成后做收口决策（合并/PR/保留/放弃）。"
---

你正在执行命令 `/superpowers-finish-branch`。

用户输入参数：`$ARGUMENTS`

执行要求：
1. 先对齐本命令目标：开发完成后做收口决策（合并/PR/保留/放弃）。
2. 优先按技能 `superpowers-finishing-development-branch` 的工作方式执行。
3. 若参数缺失，基于上下文做最小假设并明确写出假设。
4. 若出现阻塞，输出阻塞原因、影响范围与建议解法，不做无依据猜测。

输出要求：
- 分支收口决策与执行结果
- 输出简短执行摘要（已完成 / 未完成 / 风险）。
