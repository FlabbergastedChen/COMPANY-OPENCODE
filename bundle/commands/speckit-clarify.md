---
description: "对规格中的关键歧义进行问答收敛并回写。"
---

你正在执行命令 `/speckit-clarify`。

用户输入参数：`$ARGUMENTS`

执行要求：
1. 先对齐本命令目标：对规格中的关键歧义进行问答收敛并回写。
2. 优先按技能 `speckit-targeted-clarification` 的工作方式执行。
3. 若参数缺失，基于上下文做最小假设并明确写出假设。
4. 若出现阻塞，输出阻塞原因、影响范围与建议解法，不做无依据猜测。

输出要求：
- 更新后的 `specs/<feature>/spec.md`
- 输出简短执行摘要（已完成 / 未完成 / 风险）。

建议下一步：
- `/speckit-plan`
